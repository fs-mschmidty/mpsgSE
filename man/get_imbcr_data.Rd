% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imbcr_functions.R
\name{get_imbcr_data}
\alias{get_imbcr_data}
\title{Read IMBCR Data into R.}
\usage{
get_imbcr_data(fs_unit, crs = NULL, dir_path = NULL)
}
\arguments{
\item{fs_unit}{The IMBCR management unit. Use \code{see_imbcr_mgmt_units()} to
see management unit names.}

\item{crs}{Target coordinate reference system (CRS). Either and
\code{\link[sf:st_crs]{sf::st_crs()}} object or accepted input string (e.g. "NAD83").
See \code{\link[sf:st_crs]{sf::st_crs()}} for more details. Default is NULL. If NULL,
resulting sf object CRS will be NAD83.}

\item{dir_path}{Path to the IMBCR data folder. Default is NULL. If NULL, data
will be read from the MPSG T-drive folder.}
}
\value{
An \link[sf:sf]{sf::sf} class object.
}
\description{
This function reads IMBCR data into R. This function references restricted
data on the MPSG T-drive and will fail if the user is not on a Forest
Service network or the VDI and does not have access to the restricted data
folder.
}
\details{
You must be connected to the T-drive, e.g., through VPN, to use this function
unless you have the *.rda file on your local machine.
}
\examples{
## Not run:

library("mpsgSE")

mgmt_units <- c("Cimarron National Grassland", "Comanche National Grassland")
get_imbcr_data(mgmt_units)

## End(Not run)                     
}
